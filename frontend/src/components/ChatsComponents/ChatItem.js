import { View, Text } from 'react-native';

const ChatItem = () => {
    return (
        <View>
            <Text>Chat</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    avatar: {
        width: 100,
        height: 100,
    },
});

export default ChatItem;